apiVersion: apps/v1
kind: Deployment
metadata:
  name: numberone-api
  labels:
    app.kubernetes.io/name: numberone-auto-service-api
    app.kubernetes.io/component: api
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: numberone-auto-service-api
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: numberone-auto-service-api
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: numberone-api
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: numberone-auto-service-api
                    app.kubernetes.io/component: api
      containers:
        - name: numberone-api
          image: image.invalid/numberone-auto-service-api:placeholder
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: numberone-api-config
            - configMapRef:
                name: numberone-api-runtime-config
            - secretRef:
                name: numberone-api-secret
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 750m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
